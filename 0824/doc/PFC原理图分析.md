# PFC 原理图分析（Main wf4(2).pdf）

> 分析对象：`C:\Users\huyan\Documents\WXWork\1688856169509033\Cache\File\2026-08\Main wf4(2).pdf`
> 分析方法：PDF 单页 A4（842×595pt）经 PyMuPDF 提取 PFC 区域文本层 + 局部放大图识读。
> 配套代码：`HW/src/pfc_control.c`、`HW/src/llc_control.c`、`HW/src/protect.c`

---

## 1. 整体拓扑

单相 **Boost CCM PFC**，由 NCP1654 模拟闭环控制，MCU 只负责继电器时序、母线/输入电压采样、故障监控：

```
AC L/N → F1(20A熔断) → VR1(压敏) → LF1(共模 1.5mH) → X/Y电容
      → 继电器KP1旁路NTC(RT1) → DB1(整流桥 TS25P07GH)
      → L1(PFC电感 200µH) → Q3(主开关) → D5(升压二极管, SiC)
      → 母线电容(3×330µF/450V + 1µF/500V陶瓷) → VBUS
```

---

## 2. 关键器件逐项

| 位号 | 型号/参数 | 评价 |
|---|---|---|
| **U18** | NCP1654BD**133**R2G | 133 kHz **固定频率版本**（料号决定，不可外部调）。与前期频率讨论一致。 |
| **L1** | 200 µH / 52×35 | 1.5 kW@133 kHz 下估算：90Vac 满载 ΔI≈3.2 A、264Vac 满载 ΔI≈0.9 A，可保持 CCM。需确认 Isat ≥ 32 A。 |
| **Q3** | OSQ65R045K7T2F（料号看 650 V / 45 mΩ） | 导通损耗≈13 W（90Vac 满载 RMS≈17 A），加开关损耗热负荷不低，散热器需实测。 |
| **D5** | GC3D10060A（SiC 肖特基 600 V/10 A 级） | ✅ 已用 SiC，消除反向恢复损耗，是 133 kHz CCM 的合理选择。 |
| **R44//R53** | 各 0.01 Ω/2512，并联后 5 mΩ | 功耗≈1.5 W，两电阻分担可行。需按 NCP1654 数据手册换算 OCP 触发点。 |
| **母线电容** | EC3/EC4/EC9：3×330 µF/450 V 电解 + C1/C71/C72：3×1 µF/500 V 陶瓷 | 容量够 400 V 母线；电解需确认**纹波电流定额**是否满足 1.5 kW。 |
| **输入 EMI/保护** | F1 20 A、VR1 621KD20、LF1 1.5 mH、CX1/CX2/CX3、CY1~CY8 | 常规配置，无硬伤。 |
| **KP1** | HFK10-T/12-HST | 用于旁路 NTC，线圈由 MCU `RELAY_PFC_EN` 经光耦(OP3)+PNP(Q15)驱动。 |

---

## 3. 控制接口与代码对应

- **RELAY_PFC_EN**：MCU → OP3 光耦 → Q15(B772 PNP) → 继电器线圈。对应 `pfc_control.c` 的 `OFF → RELAY_ON(等待 bus≥350V) → RUN` 时序，闭合逻辑正确。
- **PFC_BRO**：AC 输入经分压系数 **0.0063** 进 MCU ADC（220 Vac 峰值 ≈ 2.11 V），即 `AC_VOL_SAMPLE_CH`。
- **PFC_FB**：母线反馈同时给 NCP1654 的 FB 脚（内部 OVP≈105%×2.5 V，对应母线约 **420 V**）与 MCU ADC。
- **HARD_PRO**：图中出现但来源未完全标清，需确认是 NCP1654 的 OVP/UVP 输出还是外部比较器——这关系到 MCU 故障锁存逻辑。

---

## 4. 风险点与建议

1. **NTC 旁路时序（高）**：RT1 为启动限流 NTC，继电器 KP1 须在 PFC 启动成功后闭合以短路 NTC。若继电器未吸合，1.5 kW 连续电流流过敏感 NTC 会快速过热烧毁。代码已按 Vbus 判据闭合，但**首板上电须实测继电器是否真吸合、NTC 是否被旁路**。
2. **MOS 热设计（高）**：Q3 45 mΩ @ 1.5 kW 导通损耗≈13 W，加开关损耗后需实测满载 30 分钟壳温/散热器温度；裕量不足则换更低 Rds(on) 的 SJ MOS 或 SiC MOS。
3. **电感 L1 饱和电流（中）**：90 Vac 满载峰值电流≈24 A，建议 L1 的 Isat ≥ 1.3× ≈ 32 A，避免低电压大电流时饱和导致电流尖峰、OCP 误触发。
4. **输出电容纹波电流（中）**：3×330 µF 电解总纹波电流定额须 ≥ PFC 输出纹波电流（1.5 kW/400 V 时约 5–8 A RMS），不足会缩短寿命。
5. **HARD_PRO 来源（中）**：确认来源与极性，避免 MCU 故障锁存逻辑与实际硬件不符。

---

## 5. 与前期认知的修正

- **12 V 辅源不是"只给风扇"**：`P12V` 还供 NCP1654 VCC、继电器线圈、光耦 LED。好在 100 mV 纹波对 NCP1654（UVLO 9–20 V）与继电器仍无碍。
- **NCP1654 频率由料号固定**：只有 65/133/200 kHz 三档，133 kHz 是选料（BD133），不能靠外接电阻"拉到 130 kHz"。
- **升压二极管已用 SiC**：前期"换 SiC 二极管损耗会不会小"的疑问，本板已落地（D5 = GC3D10060A）。

---

## 6. SiC 升级选型评估

### 6.1 升压二极管：已用 SiC ✅
GC3D10060A 消除了 CCM 硬开关的反向恢复损耗和 EMI 尖峰，收益最大、风险最低，属 PFC 标准做法。

### 6.2 主开关换 SiC MOS：损耗更小，但需权衡
- **开关损耗大幅下降**：SiC 无少数载流子存储，Eon/Eoff 远低于硅 SJ MOS；频率越高优势越放大（你选 133 kHz 正是为缩小电感，高频下硅 MOS 开关损耗上涨，SiC 能把效率拉回）。
- **导通损耗**：同耐压下 SiC Rds(on) 通常高于硅 SJ，但 1.5 kW 级新一代 SiC 已接近；高频下总损（导通+开关）更小。
- **代价**：驱动更复杂（需负压关断）、成本更高。1.5 kW 硅 SJ + SiC 二极管已成熟高效，SiC MOS 是"加分项"非"必选项"。

### 6.3 SiC 驱动电源：18 V / 100 mA 评估
- **电压 18 V**：在 SiC 推荐 +15~+20 V 区间内，能压低 Rds(on)；但多数 650 V SiC 的 **Vgs 绝对最大额定仅 ±20 V**，18 V 仅留 2 V 裕量，必须控驱动电源纹波 < 1 V 并抑制开关振铃，否则米勒尖峰顶到 20 V 有栅极击穿风险。
- **电流 100 mA**：驱动电源标的是**平均电流能力**，峰值由本地去耦电容（0.1~1 µF 陶瓷 + bulk）提供。单管 SiC 平均电流仅几~几十 mA（`I_avg ≈ Qg×f_sw`，Qg 30~80 nC × 133 kHz ≈ 数 mA 级），100 mA 对**单管足够**；若 PFC + LLC 半桥多管共用，需累加 `ΣQg×f`，但 2~3 管通常仍落在几十 mA 内。
- **单 +18 V 不能完成完整驱动**：SiC MOSFET 强烈建议**负压关断（-3 ~ -5 V）**以防米勒误开通。单 +18 V 关断是 0 V，硬开关 PFC 下有米勒导通风险；完整方案需 +18 V / -4 V 双路隔离电源或电荷泵。
- **当前板现实**：PFC 由 NCP1654 直接单电源正驱动（无负压），若只把 Q3 换成 SiC，属"能用但非最优"；严格按 SiC 规范需换专用带负压驱动，则不再是 NCP1654 直驱。

---

## 7. 上功率前验证清单

- [ ] 满载温升：Q3、D5、L1、母线电容（30 分钟实测）
- [ ] 继电器动作：确认 KP1 吸合并短路 NTC（而非 NTC 长期串联）
- [ ] 低电压大电流：90 Vac 满载，查电感饱和 / 电流尖峰 / OCP 是否误触发
- [ ] 母线 OVP/UVP：实测 NCP1654 内部 OVP(~420 V) 与 MCU 软件 OVP 是否协调
- [ ] HARD_PRO 来源与极性确认

---

_分析日期：2026-08-25。器件参数以原理图文本层提取为准，料号电气细节（Vds、Qg、Isat 等）须以原厂数据手册最终确认。_
